# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9
python-dotenv>=1.0

# --- ASR (reemplaza openai-whisper: ~4x más rápido en CPU) ---
faster-whisper>=1.1.0

# --- Análisis de señal ---
numpy>=1.26
scipy>=1.13
librosa>=0.10.2
scikit-learn>=1.5

# --- LLM ---
# LLM_PROVIDER=claude
anthropic>=1.0

# LLM_PROVIDER=gemini
# NOTA: google-genai es el SDK sucesor y google-generativeai está deprecado,
# pero llm.py todavía hace `import google.generativeai`. Cambiar la dependencia
# sin migrar el código rompe el arranque, así que se mantiene el paquete que el
# código usa hoy. La migración a google-genai queda pendiente.
google-generativeai>=0.8

# --- Diarización con ECAPA-TDNN (RECOMENDADO) ---
# Sin esto el sistema cae al fallback MFCC, que falla cuando las dos voces
# están grabadas con el mismo micrófono lejano (probado: colapsa a 1 hablante).
speechbrain>=1.0
torch>=2.2
